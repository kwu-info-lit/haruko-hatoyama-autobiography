# AGENTS.md — 鳩山春子 自叙伝 デジタルアーカイブ

このファイルはエージェ`ントがこのリポジトリで作業する際に従うべき規則と要件を定義します。

---

## プロジェクト概要

本プロジェクトは、鳩山春子（1861–1938）の自叙伝（1929年刊行）を [Re:VIEW](https://github.com/kmuto/review) 形式でデジタルアーカイブ化するものです。  
`original_images/` に格納されたスキャン画像（JPEGファイル、`001.jpg`〜`269.jpg`）を生成AIで読み取り、`contents/` 以下のRe:VIEWファイル（`.re`）を修正・校正することが主なタスクです。

---

## ディレクトリ構成

```
original_images/     # スキャン原本画像（001.jpg〜269.jpg）
contents/
  predef/            # 前付け（序文など）
    00_preface.re
  chaps/             # 各章
    00_introduction.re
    01_childhood.re
    02_moving_to_tokyo.re
    03_my_student_days.re
    04_marrying_hatoyama.re
    05_after_marriage.re
    06_becoming_a_mother.re
    07_family_and_leisure.re
    08_fathers_influence.re
    09_marquis_okuma_incident.re
    10_beauty_and_spiritual_cultivation.re
    11_hatoyama_and_talented_people.re
    12_first_election_campaign.re
    13_education_of_my_children.re
    14_memories_of_travels_abroad.re
    15_sorrow_of_life.re
    16_biography.re
catalog.yml          # 章の順序定義
config.yml           # Re:VIEW設定ファイル
```

---

## Re:VIEWフォーマット規則

`.re` ファイルは Re:VIEW 5.0 記法を使用します。主な記法：

- `= 章タイトル` — 章見出し（H1相当）
- `== 節タイトル` — 節見出し（H2相当）
- `=== 小節タイトル` — 小節見出し（H3相当）
- `@<ruby>{漢字, よみ}` — ルビ（振り仮名）
- 本文は段落ごとに1行で記述する

---

## 画像読み取りの作業手順

1. `original_images/` のJPEGファイルを生成AIのビジョン機能で読み取る
2. 読み取り結果を対応する `contents/` 内のRe:VIEWファイルの内容と照合する
3. 差異・誤りが見つかった箇所を修正する

---

## 漢字・表記に関する規則（最重要）

本プロジェクトはデジタルアーカイブが目的であるため、**原本の表記を最大限忠実に再現すること**を最優先とします。

### 漢字の表記

- **原本で使われている漢字（旧字体・異体字を含む）をできる限りそのまま使用すること**
- 現代の標準字体（新字体）に勝手に置き換えてはならない
  - 例：原本が「國」なら「国」に変えない、「學」なら「学」に変えない
- ただし、画像の解像度や文字の潰れにより判読不能な場合は、文脈から最も妥当な字体を選び、編集コメント（`#@# [要確認]`）を付与すること

### 仮名遣い

- 原本の歴史的仮名遣い（旧仮名遣い）をそのまま保持すること
  - 例：「云ふ」「居る」「居り」「やうに」「ことゆゑ」「かうふく」等
- 現代仮名遣いに変換しないこと

### 踊り字（繰り返し記号）の扱い

- **踊り字はすべて原本のまま保持すること**
- Unicode で表現可能な踊り字は展開せず、そのまま使用する
  - 「々」（U+3005）同の字点 — 例：「人々」「様々」
  - 「ゝ」（U+309D）ひらがな一つ点
  - 「ゞ」（U+309E）ひらがな一つ点（濁点付き）
  - 「ヽ」（U+30FD）カタカナ一つ点
  - 「ヾ」（U+30FE）カタカナ一つ点（濁点付き）
  - 「〱」（U+3031）／「〲」（U+3032）くの字点
  - 「〻」（U+303B）二の字点

### ルビ（振り仮名）

- 原本にルビが付いている語には `@<ruby>{語, よみ}` の形式でルビを付けること
- ルビの読みも原本の発音（旧仮名遣い）に従うこと
  - 例：`@<ruby>{刊行, かんかう}`、`@<ruby>{生活, せいくわつ}`
- 原本にルビがない語には推測でルビを付けないこと

---

## 校正時の注意事項

- 原本の句読点・括弧・記号の種類と位置を正確に再現すること
- 改行・段落分けは原本のページレイアウトではなく、原本の文意・文脈に従うこと（Re:VIEWの段落構造に合わせる）
- 章・節見出しの階層（`=`の数）は既存ファイルの構造を踏襲すること
- 数字は原本のまま（漢数字・算用数字）保持すること
- 欧文固有名詞（人名・地名）は原本の表記（カタカナ or ローマ字）に従うこと

---

## 作業記録

- 修正を加えたファイルとその変更内容は、Gitのコミットメッセージに記録すること
- 判読困難な箇所は `#@# [要確認: 理由]` のコメントをRe:VIEWファイル内に挿入すること
- 大きな不確実性がある場合は修正を行わず、ユーザーに確認を求めること

---

## 禁止事項

- 原本に存在しない文章・語句の追加
- 文意を変える意訳・要約
- 現代語への書き換え
- 字体の勝手な新字体化
- 旧仮名遣いの現代仮名遣いへの変換
- Unicode で表現可能な踊り字を展開（元の文字に戻す）こと
